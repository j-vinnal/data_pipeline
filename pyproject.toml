[project]
name = "data-pipeline"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
include = ["data_pipeline*"]

[tool.setuptools.package-data]
data_pipeline = ["config/*.toml", "config/*.yaml"]